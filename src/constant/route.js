import { prefixRoute } from './config';

export const HOME_PATH = `${prefixRoute}/`
export const PROJECT_PATH = `${prefixRoute}/project`
export const ABOUT_PATH = `${prefixRoute}/about`
export const CONTACT_PATH = `${prefixRoute}/contact`